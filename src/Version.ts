// The package version, in a module of its own so a broker can name itself on the wire without
// importing the public entry point and closing an import cycle. index.ts re-exports it.
export const STANDARD_AGENTS_VERSION = "0.1.0";
